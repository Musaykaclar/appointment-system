@using MudBlazor

<MudDialog>
    <DialogContent>
        <MudText>@Message</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">@CancelText</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Submit">@ConfirmText</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [Parameter] public string Message { get; set; } = string.Empty;
    [Parameter] public string ConfirmText { get; set; } = "Onayla";
    [Parameter] public string CancelText { get; set; } = "İptal";
    
    [Parameter] public EventCallback<bool> OnResult { get; set; }

    async Task Submit() 
    {
        await OnResult.InvokeAsync(true);
    }
    
    async Task Cancel() 
    {
        await OnResult.InvokeAsync(false);
    }
}